namespace Xim.Simulators.Api.Routing
{
    internal enum RouteOrder
    {
        Action = 0,
        ActionNoQuery = 1,
        Route = 2,
        RouteNoQuery = 3,
        None = int.MaxValue
    }
}
